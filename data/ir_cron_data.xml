<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Scheduled Action: Send Pending Attendee Reminders -->
        <record id="ir_cron_send_pending_attendee_reminders" model="ir.cron">
            <field name="name">Event Ticket Store: Send Attendee Details Reminders</field>
            <field name="model_id" ref="sale.model_sale_order" />
            <field name="state">code</field>
            <field name="code">model._cron_send_pending_attendee_reminders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="False" />
            <field name="doall" eval="False" />
            <field name="priority">100</field>
        </record>

    </data>
</odoo>

